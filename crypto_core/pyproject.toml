[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "crypto-core"
version = "0.1.0"

[tool.setuptools]
py-modules = ["cost", "safety", "reliability", "speed", "weights", "normalizer"]